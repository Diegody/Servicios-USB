Column(
  crossAxisAlignment: CrossAxisAlignment.start,
  children: _sessionDetails.map((student) {
    return Container(
      margin: EdgeInsets.symmetric(vertical: 5),
      padding: EdgeInsets.all(10),
      decoration: BoxDecoration(
        border: Border.all(
          color: Colors.orange, // Puedes cambiar el color del borde según tus preferencias
          width: 2,
        ),
        borderRadius: BorderRadius.circular(8),
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            'Código: ${student['CEDULA']}',
            style: TextStyle(
              fontSize: 16,
              fontWeight: FontWeight.bold,
            ),
          ),
          Text(
            'Nombre: ${student['NOMBRES']}',
            style: TextStyle(fontSize: 14),
          ),
          Row(
            children: [
              Checkbox(
                value: false,
                onChanged: (bool? value) {
                  // Implementa lógica para manejar la asistencia aquí
                },
              ),
              SizedBox(width: 5),
              Text('Asistencia'),
            ],
          ),
        ],
      ),
    );
  }).toList(),
),
